package Ejercicios;

public class Cuarto_Scanner {

    public static void main(String[] args) {
        /*
         * Enunciado del ejercicio:
         *
         * Crea un programa en Java que interactúe con el usuario usando la clase Scanner.
         * El objetivo es que practiques lo siguiente:
         *
         * 1. Leer diferentes tipos de datos desde la consola.
         * 2. Manejar el flujo de datos del Scanner de manera correcta.
         * 3. Validar la entrada de datos para evitar errores.
         *
         * Instrucciones:
         *
         * 1. Solicita al usuario la siguiente información, en el mismo orden:
         *    a. Su nombre completo (usa nextLine()).
         *    b. Su edad (usa nextInt()).
         *    c. Su altura en metros (usa nextFloat()).
         *    d. Su inicial del nombre (solo la primera letra del nombre, usa next().charAt(0)).
         *    e. Si está de acuerdo o no en continuar (usa nextBoolean() para leer true o false).
         *    f. Un número entero grande, mayor que 100,000 (usa nextLong()).
         *    g. Su peso en kilogramos (usa nextDouble()).
         *
         * 2. Asegúrate de manejar correctamente el salto de línea tras usar nextInt() y nextFloat().
         *
         * 3. Valida que el usuario introduzca datos correctos.
         *    a. Si el usuario introduce un tipo de dato incorrecto (por ejemplo, un texto en lugar de un número),
         *       deberás mostrar un mensaje de error y pedirle que vuelva a introducir un valor válido.
         *
         * 4. Imprime todos los datos introducidos por el usuario al final del programa, mostrando un resumen.
         *
         * Bonus:
         * - Si quieres, añade algún manejo de excepciones para capturar errores de entrada.
         *
         * ¡Buena suerte!
         */
    }
}
